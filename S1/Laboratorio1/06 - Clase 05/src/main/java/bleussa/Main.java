package bleussa;

import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        // PUNTO 2

            // Algoritmo ar = new Algoritmo();

            // GENERAMOS UN ARRAY DESORDENADO
            // int[] listaDesordenada = {10,62,64,25,36,98,15,9,5,36};
            // System.out.println("ARRAY ORIGINAL: ");
            // System.out.println(Arrays.toString(listaDesordenada));

            // ORDENAMIENTO POR INSERCIÓN
            // System.out.println("INSERCION:");
            // ar.algoritmoInsersion(listaDesordenada);
            // System.out.println(Arrays.toString(listaDesordenada));

            // ALGORITMO DE BURBUJA
            // System.out.println("BURBUJA:");
            // ar.algoritmoBurbuja(listaDesordenada);
            // System.out.println(Arrays.toString(listaDesordenada));

            // ALGORITMO DE SELECCION
            // System.out.println("SELECCION:");
            // ar.algoritmoSeleccion(listaDesordenada);
            // System.out.println(Arrays.toString(listaDesordenada));

            // ALGORITMO DE BUSQUEDA SECUENCIAL
            // System.out.println("BUSQUEDA SECUENCIAL:");
            // ar.algoritmoBusquedaSecuencial(listaDesordenada, 5);



        // PUNTO 3 y 4
        /* Codifique un algoritmo que permita cargar un array de una dimensión de 20 elementos
        enteros. Finalizada la carga de los 20 elementos el programa debe solicitar como se
        desea ordenar el array ASCENDENTE O DESCENDENTE y que método de ordenamiento aplicar
        (inserción, burbuja, selección), según las opciones elegidas aplique el algoritmos
        fundamental de ordenamiento que corresponda. Muestre por pantalla el array original
        desordenado y su resultante ordenado según las opciones elegidas. */
        /*
            Array arr = new Array();
            int[] arraycreado = arr.crearArray();   // CREAMOS EL ARRAY Y LO CARGAMOS DE DATOS
            arr.mostrarAlgoritmo(arraycreado);      // MOSTRAMOS EL ARRAY ORIGINAL
            arr.ordenarArray(arraycreado);          // ORDENAMOS EL ARRAY SEGUN PIDA EL USUARIO
            arr.mostrarAlgoritmo(arraycreado);      // MOSTRAMOS EL ARRAY MODIFICADO
        */



        // PUNTO 5
        /* Codifique un algoritmo que solicite 2 valores enteros mayores o iguales a 2, que se
        corresponden a cantidad de filas y columnas. Cree un arreglo de tamaño[X,Y] de tipo
        entero, el tamaño se corresponde a los 2 valores enteros ingresados inicialmente.
        Solicite al usuario que ingrese números enteros para cargar cada una de las celdas de
        la matriz. Finalizada la carga, calcule el promedio entero de los elementos asignados y
        cargados en la matriz. Para terminar determine si el promedio calculado se encuentra
        asignado en alguna de las celdas, si es así indique la posición o posiciones
        correspondientes, caso contrario muestre el mensaje “No se encontró ninguna
        coincidencia”. */

        // Punto5 p5 = new Punto5();
        // p5.crearArray();

    }

}