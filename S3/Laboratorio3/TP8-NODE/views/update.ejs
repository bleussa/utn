<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TP NODE</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/flatly/bootstrap.css">
</head>
<body>
    <div class="container" style="margin-top: 50px;">
        <div style="display: flex; align-items: center; justify-content: space-between;">
            <h1>Modificar Empleado <%= results[0].legajo %></h1>
        </div>
        <form action="/updateEmpleado" method="post">
                <div class="row">
                    <div class="mb-3 col">
                        <label for="legajo" class="form-label">Legajo</label>
                        <input type="number" class="form-control form-control-sm" id="legajo" name="legajo" required value="<%= results[0].legajo %>" readonly>
                    </div>
                    <div class="mb-3 col">
                        <label for="apellido" class="form-label">Apellido</label>
                        <input type="text" class="form-control form-control-sm" id="apellido" name="apellido" value="<%= results[0].apellido %>" required>
                    </div>
                    <div class="mb-3 col">
                        <label for="nombre" class="form-label">Nombre</label>
                        <input type="text" class="form-control form-control-sm" id="nombre" name="nombre" value="<%= results[0].nombre %>" required>
                    </div>
                    <div class="mb-3 col">
                        <label for="dni" class="form-label">DNI</label>
                        <input type="number" class="form-control form-control-sm" id="dni" name="dni" required value="<%= results[0].dni %>">
                    </div>
                </div>
                <div class="row">
                    <div class="mb-3 col">
                        <label for="sector" class="form-label">Sector</label>
                        <input type="text" class="form-control form-control-sm" id="sector" name="sector" value="<%= results[0].sector %>" required>
                    </div>
                    <div class="mb-3 col">
                        <label for="fechaIngreso" class="form-label">Fecha de Ingreso</label>
                        <input type="text" class="form-control form-control-sm" id="fechaIngreso" name="fechaIngreso" value="<%= results[0].fechaIngreso %>" required>
                    </div>
                    <div class="mb-3 col">
                        <label for="activo" class="form-label">Activo</label>
                        <select class="form-select form-select-sm" name="activo" id="activo" required>
                            <% if (results[0].activo == 1) { %>
                                <option value="1" selected>Activo</option>
                                <option value="0">Inactivo</option>
                            <% } else { %>
                                <option value="1">Activo</option>
                                <option value="0" selected>Inactivo</option>
                            <% } %>
                        </select>
                    </div>
                </div>
                <input type="submit" value="Guardar" class="btn btn-primary">
                <a href="/" class="btn btn-secondary">Volver atras</a>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>